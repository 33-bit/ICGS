# Generation runtime hardening — local verification record

Status: **Task 7 local verification complete; Task 8 remote acceptance pending**.
This record covers the committed hardening work through `ff04104` and is the
local evidence checkpoint before any separately authorized CPU/simulator/HF
validation run.

## Scope and safety boundary

The local checks below used the repository-owned Python tests and L0 harness
only. No Colab session, simulator, download, generation workload, robot motion,
or Hugging Face publication was launched.

Validation plans remain JSON data consumed by the existing generation modules;
there is no executable validation script. The bounded validation plan uses two
workers and the isolated `validation/validation-cpu-20260922/` prefix.

## Environment

| Check | Environment |
| --- | --- |
| Focused/full tests | Python 3.11.15 from `.venv` |
| L0 | Python 3.14.4 |
| Platform | Darwin arm64 (`Huys-MacBook-Pro.local`) |
| Baseline commit | `ff04104a48a46aedf5ce6ec965cdff276c03013b` |

## Evidence

| Gate | Status | Exact evidence or reason |
| --- | --- | --- |
| Focused generation suite | PASS | `PYTHONPATH=src .venv/bin/python -B -m pytest -q tests/test_generation.py tests/test_generation_*.py` — 180 passed, 0 failed, 1.01s |
| Full repository-owned tests | PASS | `PYTHONPATH=src .venv/bin/python -B -m pytest -q tests` — 859 passed, 10 skipped, 3 warnings, 42.89s, exit 0 |
| Full-suite skipped tests | SKIPPED | 10 tests reported by pytest; skips remain explicit and are not counted as passes |
| L0 syntax/docs/harness checks | PASS | `python3 -B scripts/validate_fast.py` — Python syntax, local links, harness boundary, canonical generation naming, and 22 harness tests passed |
| L1 CPU smoke | NOT RUN | Requires the separately authorized CPU validation workload |
| L2 model validation | NOT RUN | Requires model execution outside this local checkpoint |
| L3 simulator validation | NOT RUN | No simulator was launched |
| L4 benchmark validation | NOT RUN | No benchmark workload was launched |
| Colab/session validation | NOT RUN | No Colab session was created or contacted |
| HF publication/remote hash validation | NOT RUN | No download, remote inspection, or publication was performed |
| Diff hygiene | PASS | `git diff --check` exited 0 |
| Generated bytecode cleanup | PASS | `.pyc`/`.pyo` scan is empty after removing test-generated `__pycache__` bytecode |

The full-suite warnings were the existing pytest collection warning for
`TestRecorder` and two existing invalid-escape warnings in `test_task_router.py`;
they did not cause test failures.

## Remaining risk

The local evidence validates contracts, queue isolation, recovery logic,
publication reconciliation, and bounded receipt semantics without exercising a
real simulator, worker process, coordinator restart, or remote repository.
Those gates remain `NOT RUN` until Task 8 is explicitly authorized and its
isolated receipts are recorded.

## Task 8 first acceptance attempt — FAIL

The first authorized CPU run is recorded in
`docs/experiments/generation-validation/validation-cpu-20260922/`. Provisioning
and 92 remote contract tests passed on a two-worker CPU session, but the run
was stopped safely after the coordinator refilled 400 jobs despite the JSON
plan's `max_jobs=7` bound. The receipt records 27 simulator-crash attempts,
zero publication, and the watchdog's `pid_invalid` coordinator restart. The
session was stopped and the server assignment list is empty. The active plan
remains open until the refill bound is enforced and a new bounded acceptance
run completes every required gate.
